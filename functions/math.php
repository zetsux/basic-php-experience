<?php 
    /*
        Ref Docs : https://www.php.net/manual/en/book.math

        Frequently used :
        abs(x), pow(a, n), sqrt(x), max(a, b), min(a, b), 
        round(x), ceil(x), floor(x), pi(), rand(a, b)
    */
?>

